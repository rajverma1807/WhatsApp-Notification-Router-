"""Placeholder voice-processing helpers for future speech and audio integrations."""

from __future__ import annotations

from pathlib import Path
from typing import Any


def transcribe_voice_note(voice_path: str | Path) -> str:
    """Transcribe a voice note, currently a placeholder."""
    return ""


def classify_voice_note(voice_path: str | Path) -> str:
    """Classify a voice note, currently a placeholder."""
    return ""
