"""Placeholder image-processing helpers for future OCR and vision integrations."""

from __future__ import annotations

from pathlib import Path
from typing import Any


def extract_text_from_image(image_path: str | Path) -> str:
    """Return OCR-like text from an image, currently a placeholder."""
    return ""


def caption_image(image_path: str | Path) -> str:
    """Create an image caption, currently a placeholder."""
    return ""


def understand_poster(image_path: str | Path) -> str:
    """Summarize poster content, currently a placeholder."""
    return ""
